## Infrastructure Overview
This folder contains the Docker Compose configuration used to deploy the HAPI FHIR server. The docker-compose.yml file defines the container image, port mapping, restart policies, and persistent storage volumes required to operate a stateful FHIR warehouse environment.

